@import './reset'
@import './fonts'
@import './loco.styl'

* {
	user-select: none;
}

a {
	text-decoration: none;
}

.explosion-icon-container {
	position absolute
	top 50%
	left 50%
	transform translate(-50%, -50%)

	.explosion-icon {
		opacity 0
		width 20px
	}
}
